unit CFX.Controls;

interface
  uses
    Vcl.Graphics, Classes, Types, CFX.Types, CFX.UIConsts, SysUtils,
    CFX.Graphics, CFX.VarHelpers, CFX.ThemeManager, Vcl.Controls,
    CFX.PopupMenu;

  type
    // Control
    FXControl = class(TControl)
    private
      FPopupMenu: FXPopupMenu;
    end;

    // Win Control
    FXWinControl = class(TWinControl)

    end;

implementation

end.
